// Marketing is independent of PUBLIC_ORIGIN and the Local tunnel.
// Set VITE_WEBSITE_URL after the standalone site hostname is assigned.
export const WEBSITE_URL = import.meta.env.VITE_WEBSITE_URL?.trim() || undefined
export const REPOSITORY_URL = 'https://github.com/nikdelvin/nathing-app'
